{
  "buildCommand": "prisma generate && (prisma db push --accept-data-loss || echo 'db push skipped') && (npx tsx prisma/seed.ts || echo 'seed skipped') && next build",
  "crons": [
    {
      "path": "/api/cron/fetch-news",
      "schedule": "0 10 * * *"
    },
    {
      "path": "/api/cron/fetch-licitacoes",
      "schedule": "0 11 * * *"
    }
  ]
}
